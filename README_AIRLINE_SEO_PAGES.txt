BoardBagFees airline SEO page build

Created 32 airline detail pages under /airlines/.
The pages use unique static titles/descriptions/headings and load current policy data from the existing Supabase-backed /api/airlines endpoint.
The homepage airline names now link to the detail pages.
The sitemap now contains 33 URLs (homepage + 32 airline pages).

When you add more airlines to Supabase, generate matching detail pages and add them to sitemap.xml before expecting them to be indexed.
